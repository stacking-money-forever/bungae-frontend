FROM CODEX
Run a fresh, isolated scenario QA of the supplied preview URL as a first-time adult participant. Your goal is to find a suitable small meetup within 24 hours, join it, and understand your participation state after returning. Use only the visible app. Do not read app source, earlier QA sessions/reports, hidden state, browser tokens, or undocumented APIs. Write your own understanding readback and feasibility preflight before interaction, and preserve blind first-impression notes before DOM assistance.

Use one fresh Ego Lite TaskSpace. A synthetic QA-only account is available in `/tmp/bungae-blind-qa-replacement-20260914-q0dh5mfs/actor-qa3-login.json`; read only this handoff for authentication without printing its values in commands, logs, evidence, screenshots, or reports. Never use personal numbers, real SMS/KCP, another account, or production data. Create or change only disposable QA meetup/participation records. No payments, real messages, publication, app edits, or external account changes.

Evidence mechanics: read `app-qa`, its actor and runtime contracts, and `ego-browser`. Use `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-participant-20260914/qa_event.py` for every browser interaction: `begin` before acting, then `end` immediately after with the observed result. It copies the exact generated attempt ID and automatically records the required checkpoint after each result. Do not hand-type IDs. For contract, preflight, blind notes, and viewed screenshots, use the canonical evidence helper directly. Before the final report, use `qa_event.py finish` with an actual linked result ID and verify that its validator reports `valid=true`; never rewrite invalid evidence or invent a result. If blocked, report the exact boundary as partial.

Within the supplied runtime and action limits, prioritize completing the core join and reload path; then check reachable error/recovery, keyboard and narrow viewport edges. Distinguish browser-runner failure from app behavior and tested behavior from user sentiment. Write `actor-report.md` and `actor-report.json` under the supplied evidence root with exact receipt, tested/skipped scope, observed vs interpretation, and remaining risk. No defect claim without independent oracle and fresh-state reproduction.
