FROM CODEX
Run a fresh blind scenario QA of the supplied preview URL. You are a first-time adult participant looking for a suitable small meetup within the next 24 hours. Your goal is to join and understand your status on return, using only the visible app. Do not read app source, prior QA runs or reports, hidden state, browser tokens, or undocumented APIs. Start with your own understanding readback and feasibility preflight; preserve first-impression notes before DOM-assisted inspection.

Use one fresh isolated Ego Lite TaskSpace. A synthetic QA-only account is in `/tmp/bungae-blind-qa-replacement-20260914-q0dh5mfs/actor-qa3-login.json`. Read only that handoff for login, without printing its values in commands, logs, evidence, screenshots, or reports. Do not use personal numbers, real SMS/KCP, another account, or production data. Create or change only disposable QA meetup/participation records. No payments, real messages, publication, app edits, or external account changes.

Evidence correctness is part of this task. For each browser interaction, append one `attempted` event, capture its returned exact ID, then append exactly one linked `success`, `failed`, or `blocked` result with that ID. Never use placeholders such as `__LAST__`. Checkpoint before exceeding each six-action batch, with actual completed-action and remaining-budget counts. Validate the journal before the final report; if validation fails, preserve it and report partial without rewriting or fabricating events. Only use `tested-pass`, `tested-fail`, or `blocked` as the core-flow status and never claim `qa-complete` without a tested core flow and valid evidence.

Stay within the supplied 24-action and 900-second limits. Exercise reachable normal, error/recovery, reload, narrow-viewport, and keyboard behavior without guessing missing states. Write `actor-report.md` and `actor-report.json` under the supplied evidence root, separating observable UI behavior, interpretation, and untested business/user-sentiment claims.
