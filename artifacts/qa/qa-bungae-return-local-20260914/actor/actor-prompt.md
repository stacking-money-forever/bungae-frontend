FROM CODEX
Run a fresh isolated scenario QA at the supplied local URL as a returning adult participant in a small meetup. Your goal is to find your current participation state, reload the page, and regain the same state if authentication is requested. Use only visible app controls. Also check the critical path with keyboard input and at a 390×844 viewport if reachable. Do not read app source, previous QA sessions or reports, hidden state, browser tokens, or undocumented APIs. Write your own understanding readback and feasibility preflight before browser interaction, and blind first-impression notes before DOM-assisted inspection.

Use one fresh Ego Lite TaskSpace. A synthetic QA-only returning-participant account is in `/tmp/bungae-blind-qa-replacement-20260914-q0dh5mfs/actor-qa3-login.json`; read only this handoff for login without printing values in commands, logs, evidence, screenshots, or reports. This identity is intentionally reused from a completed earlier session to test return behavior, but do not import that browser/session/context. Do not use personal numbers, real SMS/KCP, another account, or production data. Only inspect or change disposable QA meetup/participation records. No payments, real messages, publication, app edits, or external account changes.

Read the `app-qa` Actor/runtime contracts and `ego-browser` before Ego operations. Use `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-participant-20260914/qa_event.py` `begin` before each browser interaction and `end` immediately after; it links exact IDs and checkpoints every result. Record readback, preflight, blind notes, and any viewed screenshots with the canonical evidence helper. Do not hand-type evidence IDs. Finish with a real linked result ID and validate the journal before reporting; never rewrite invalid evidence or invent a result.

Prioritize the complete return/re-authentication/participation-state path within the supplied action and runtime budget; then exercise keyboard and narrow viewport if remaining. Write `actor-report.md` and `actor-report.json` under the supplied evidence root, separating observed behavior, interpretation, and untested claims. Include the exact receipt, URL, tested/skipped scope, and evidence validation result. Do not claim a backend or business defect without an independent oracle and fresh-state reproduction.
