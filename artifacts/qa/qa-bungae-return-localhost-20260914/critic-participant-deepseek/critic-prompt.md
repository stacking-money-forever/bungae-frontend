FROM CODEX
Independently QA the returning PARTICIPANT path on exactly `http://localhost:3117` with the supplied synthetic QA-1 participant identity. First read only `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-return-localhost-20260914/actor/actor-visible-sanitized.md` and write `first-judgment.md` in your evidence root before browser/DOM inspection. Do not read raw Actor evidence, earlier QA sessions/reports, source, bundles, private stores, browser tokens, or undocumented APIs. No Actor screenshot is valid.

Use one fresh isolated Ego Lite TaskSpace. For authentication read ONLY `phone_number` and `qa_code` from `/tmp/bungae-blind-qa-handoff-20260914-60iw_sal/actor-login.json`; ignore its `preview_url`. Never print authentication values. QA-1 is an existing participant in a disposable QA meetup and is distinct from the Actor's QA-3. No personal/production data, payments, messages, publication, app edits, or external account changes. After every navigation, verify the Page origin remains `http://localhost:3117`; stop if it changes.

After the first judgment, run `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-participant-20260914/qa_event.py critic-setup` with your output, worker, launcher receipt, nonempty first-judgment blind notes, `--oracle-sources 'visible local navigation and participation state transitions'`, and `--unsupported-claims 'backend transaction truth, human comprehension, visual geometry without screenshot'`. This records the only blind note and oracle audit. Do not add a second oracle audit. Use the helper's `begin`/`end` for each browser action, then validate the journal against the launcher receipt. Keep exact generated IDs, no handwritten placeholders or event rewrites.

Read `app-qa` Critic/runtime contracts and `ego-browser` before Ego operations. Use `page.goto` only once for the supplied initial URL. Subsequently follow only user-visible app links/buttons with pointer or keyboard; do not jump routes by direct `goto` or URL assignment. Login, open the current participant meetup through visible navigation, record its `OPEN` state and `모임 참여 취소하기` action, reload that exact detail URL, follow the visible login control, reauthenticate, and check that the SAME detail URL, `OPEN` state, and `모임 참여 취소하기` action return. Use no more than the two OTP requests needed by this scenario. If an OTP request is rate-limited, record the visible response once and stop rather than retrying within this run. At 390×844 make visual claims only if a real screenshot is captured and viewed; otherwise mark visual untested.

Write `critic-report.md` and `critic-report.json` DIRECTLY under the supplied evidence root. Include actual route/origin at each checkpoint, evidence IDs, observed/interpretation/friction, eight Critic scores, tested/skipped scope, and validator result. The core outcome is the participant-specific action before and after reauthentication, not just generic route restoration.
