# Critic first judgment

## Understanding readback
I will independently exercise the returning synthetic participant flow only on `http://localhost:3117` in one fresh isolated Ego Lite TaskSpace. I will verify the first page, current participant state, reload re-authentication gate, and return to the same meetup with the participant action restored. I will use only the sanitized Actor handoff for initial user-visible context and only the phone/code fields from the synthetic login handoff. I will not navigate the supplied preview URL, inspect source/bundles/private stores/tokens/undocumented APIs, or perform destructive or real-world actions. I will record every browser action and check the visible origin after navigation. Pointer and keyboard activation plus 390×844 are conditional checks.

## Feasibility preflight
The shortest credible core flow is reachable if localhost:3117 serves the app and the synthetic account can authenticate using the handoff credentials. The required state is an existing joined participant with a current meetup; the sanitized handoff indicates that state but is not an oracle. Observable UI behavior will be judged from the live page and linked evidence. Business/backend transaction truth is outside this run unless independently supported by an allowed oracle. The independent oracle for this Critic is the executable observable contract: after re-authentication, the same meetup detail must visibly expose the participant action; business truth claims remain untested. No additional state creation is needed and no destructive actions are permitted.

Preflight result: ready for bounded browser interaction, subject to origin checks and runtime availability.
