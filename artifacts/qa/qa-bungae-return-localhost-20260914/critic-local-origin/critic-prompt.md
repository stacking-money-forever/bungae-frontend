FROM CODEX
Independently QA the returning-participant path on exactly `http://localhost:3117`. The supplied launcher target is the ONLY navigation origin for this run. Before browser diagnostics read only `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-return-localhost-20260914/actor/actor-visible-sanitized.md` and write `first-judgment.md` in your evidence root. No valid Actor screenshot exists. Do not read the Actor raw journal, prior QA sessions/reports, source, bundles, browser tokens, private stores, or undocumented APIs.

Use one fresh isolated Ego Lite TaskSpace. For authentication, read ONLY `phone_number` and `qa_code` from `/tmp/bungae-blind-qa-handoff-20260914-60iw_sal/actor-login.json`; deliberately ignore its `preview_url` field. That field is an old deployed preview and MUST NOT be navigated to or used as the target. Do not print authentication values in commands, logs, evidence, screenshots, or reports. After every navigation, check the visible Page URL origin: it must remain exactly `http://localhost:3117`. If it changes, stop that path and report the first changed URL origin without credentials. The account is a separate synthetic existing participant; use only disposable QA data. No personal/production data, payments, real messages, publication, or app edits.

Read `app-qa` Critic/runtime contracts and `ego-browser` before Ego operations. In your fresh local-origin browser, independently verify current participant state, reload, visible re-authentication, and return to the same meetup with the participant action restored. Test pointer and keyboard activation of the critical return control if reachable; try 390×844 layout only if browser screenshot capture works. Record one `oracle-audit` before findings. For every browser interaction use `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-participant-20260914/qa_event.py` `begin`/`end`; validate journal against the launcher receipt before reporting.

Write `critic-report.md` and `critic-report.json` DIRECTLY under the supplied evidence root. State the actual URL origin at the first page, meetup detail, post-reload gate, auth page, and post-auth destination; this origin proof is required for acceptance. Separate observations, interpretation, friction, and untested claims; score the eight Critic dimensions with evidence. Do not claim backend/business truth from visible state alone.
