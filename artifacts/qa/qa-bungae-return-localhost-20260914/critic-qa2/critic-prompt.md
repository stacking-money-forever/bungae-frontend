FROM CODEX
Independently QA the local meetup-detail return mechanism on exactly `http://localhost:3117`. First read only `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-return-localhost-20260914/actor/actor-visible-sanitized.md` and write `first-judgment.md` directly in your evidence root before browser/DOM inspection. Do not read raw Actor evidence, earlier QA sessions/reports, source, bundles, private stores, browser tokens, or undocumented APIs. No Actor screenshot is valid.

Use one fresh isolated Ego Lite TaskSpace and QA-2, a synthetic meetup organizer account, from `/tmp/bungae-blind-qa-handoff-20260914-60iw_sal/critic-login.json`. Read ONLY `phone_number` and `qa_code`; ignore the `preview_url` field. Never print authentication values. QA-2 has a different role from the Actor's participant account: your independent check is route/state restoration for an organizer, not a claim about the participant-specific action. Use only disposable QA data. No personal/production data, payments, messages, publication, app edits, or external account changes. Verify every observed page origin remains `http://localhost:3117`.

After the first judgment, run `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-participant-20260914/qa_event.py critic-setup` with your output, worker and receipt, nonempty `--blind-notes`, `--oracle-sources 'visible local navigation and meetup state transitions'`, and `--unsupported-claims 'backend truth, participant-specific action, human comprehension, visual geometry without screenshot'`. This records the one required blind note and valid `runtime-assertion` oracle audit. Do not add another. Use the same helper's `begin`/`end` for each browser action and validate the journal against the launcher receipt before reporting.

Read `app-qa` Critic/runtime contracts and `ego-browser` before Ego operations. Use `page.goto` only for the initial supplied URL, then follow visible links/buttons with pointer or keyboard actions; do not jump between app routes by `goto` or direct URL assignment. Open QA-2's current meetup detail via visible navigation, record the visible organizer state/action, reload, use the visible login control, reauthenticate, and check whether the SAME detail URL and organizer state/action are restored. Do not make more OTP requests than this scenario requires; if rate-limited, record the exact visible response and stop. Try 390×844 visual inspection only if screenshot capture and viewing actually work.

Write `critic-report.md` and `critic-report.json` DIRECTLY under the supplied evidence root. Include origin and route at each checkpoint, evidence IDs, observed/interpretation/friction, eight Critic scores, tested/skipped scope and validator result. State clearly that the Actor separately verified the participant path; this run verifies or fails the shared organizer return mechanism.
