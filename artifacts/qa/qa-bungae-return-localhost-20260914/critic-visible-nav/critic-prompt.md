FROM CODEX
Independently QA the returning-participant journey on exactly `http://localhost:3117`. The launcher URL is the only permitted navigation origin. First read only `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-return-localhost-20260914/actor/actor-visible-sanitized.md` and write `first-judgment.md` directly under the supplied evidence root before browser/DOM inspection. Do not read raw Actor evidence, earlier QA sessions/reports, source, bundles, private stores, browser tokens, or undocumented APIs. No Actor screenshot is valid.

Use one fresh isolated Ego Lite TaskSpace. For login read ONLY `phone_number` and `qa_code` from `/tmp/bungae-blind-qa-handoff-20260914-60iw_sal/actor-login.json`; ignore its old `preview_url` entirely. Never print authentication values. This account is a separate synthetic existing participant. Use only disposable QA data; no personal/production data, payments, real messages, publication, app edits, or external account changes. Verify the Page origin is `http://localhost:3117` at first page, auth, meetup detail, reload gate, and post-auth destination; stop if the origin changes.

After writing the first judgment, run `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-participant-20260914/qa_event.py critic-setup` with your output, worker, launcher receipt, nonempty blind notes from your first judgment, `oracle_sources=visible controls and state transitions in this local QA run`, and `unsupported_claims=backend truth, human comprehension, visual geometry without screenshot`. That command creates the required blind note and valid `runtime-assertion` oracle audit. Do not add another oracle audit. Use the same helper's `begin`/`end` for every browser interaction; it links exact IDs and checkpoints automatically. Validate your journal at the end. Preserve it if invalid; never rewrite or fabricate events.

Use `page.goto` only for the first supplied URL. Thereafter follow user-visible links/buttons with pointer or keyboard actions; do not use `goto` or direct URL assignment to jump between app pages, because this scenario tests normal in-app navigation. Use `reload` only at the requested return checkpoint. As an independent existing participant, authenticate, open the current meetup through visible navigation, note the participant action, reload, reauthenticate through the visible control, and check whether the same meetup and action are restored. At 390×844 inspect the layout if a real screenshot can be captured and viewed; otherwise mark visual results untested. Do not infer backend transaction truth from the screen.

Write `critic-report.md` and `critic-report.json` DIRECTLY under the supplied evidence root. Include actual URL origin at each checkpoint, evidence IDs, observed/interpretation/friction, the eight Critic scores, tested/skipped scope, and validator result. The core outcome is pass/fail of the complete visible return flow, not a guess about implementation.
