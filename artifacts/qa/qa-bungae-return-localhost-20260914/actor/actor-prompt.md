FROM CODEX
Run a fresh isolated scenario QA at the supplied local URL as a returning adult participant. Find the current meetup participation state, reload, and regain that same state if authentication is requested. Use only visible app controls. If reachable, check the key controls with keyboard input and a 390×844 viewport. Do not read source, prior QA runs/reports, hidden state, browser tokens, or undocumented APIs. Write your own understanding readback and feasibility preflight before browser interaction, and blind first-impression notes before DOM-assisted inspection.

Use a fresh Ego Lite TaskSpace. A synthetic QA-only returning-participant account is in `/tmp/bungae-blind-qa-replacement-20260914-q0dh5mfs/actor-qa3-login.json`; read only this handoff for login without printing values in commands, logs, evidence, screenshots, or reports. This identity was used in a completed earlier session solely so returning-participant state exists; never import that browser/session/context. No personal numbers, real SMS/KCP, other accounts, production data, payments, messages, publication, or app edits. If a browser action unexpectedly puts an authentication value into a visible URL, stop that path and report the runner/environment issue without repeating it.

Read `app-qa` Actor/runtime contracts and `ego-browser` before Ego operations. Use `/Users/justn/dev/.worktrees/bungae-frontend-participant-flow/artifacts/qa/qa-bungae-participant-20260914/qa_event.py` `begin` before each interaction and `end` immediately after; it binds exact IDs and checkpoints every result. Record readback, preflight, blind notes, and viewed screenshots with the canonical helper. Finish with an actual linked result ID and validate the journal. Never rewrite invalid evidence or invent a result.

Prioritize the complete return/re-authentication/participation-state path within the supplied caps. Write `actor-report.md` and `actor-report.json` under the evidence root with exact receipt, URL, tested/skipped scope, observations, interpretation, and validation. Do not claim a backend/business defect without an independent oracle and fresh-state reproduction.
