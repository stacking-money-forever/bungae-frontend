# Returning-participant Actor handoff

Target: `http://localhost:3117`, frontend worktree `codex/participant-flow-evidence` at HEAD `de21c76e1f7afd99b15fd75e3f5e6ac15f21dfe9` plus uncommitted changes. Actor evidence journal validated: 55 events, 17 linked actions, no errors. This file contains only user-visible observations and no authentication values or session data. It is not an independent oracle.

- A previously joined synthetic account showed one current meetup with `OPEN` and `모임 참여 취소하기`.
- Reload of its detail URL showed `로그인 필요` and withheld details.
- Visible phone/code re-authentication returned to that same meetup URL. `OPEN` and `모임 참여 취소하기` reappeared. Core-flow result ID: `1f5f56a0f5514ddb97207f23f2f5ad96`.
- Keyboard activation worked for key controls. Two pointer attempts were intercepted by visible navigation or overlay layers; no confirmed root cause.
- At 390×844 the detail semantics remained reachable. Ego screenshot capture timed out, so no screenshot-based visual claim is available.

Backend transaction truth, visual geometry, and any real person's understanding were not established by this Actor run.
