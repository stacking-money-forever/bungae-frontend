# Agent Rules

## 기준 문서

- 화면과 컴포넌트의 원본은 [bungae-design](https://github.com/stacking-money-forever/bungae-design)의 `벙개_MVP.pen`이다.
- `.pen` 파일은 직접 파싱하거나 편집하지 않는다. 디자인 변경은 Pencil MCP에서 수행한다.
- 제품 동작과 정책은 디자인 저장소의 기능 명세서와 페이지 체크리스트를 따른다.
- 디자인 토큰, 타이포그래피, 컴포넌트 계약은 디자인 저장소의 `DESIGN.md`를 기준으로 한다.

## 변경 규칙

- 변경 전에 `git status`로 기존 작업을 확인한다. 예상하지 못한 변경을 되돌리거나 삭제하지 않는다.
- 기존 프론트엔드 도구와 패턴을 우선 사용한다. 필요한 경우에만 의존성을 추가한다.
- 기능·상태·오류·권한 흐름을 생략한 목업이나 no-op 구현을 추가하지 않는다.
- 경고와 실패를 숨기지 않는다.

## 검증

- 변경된 동작을 검증하는 테스트를 함께 추가하거나 갱신한다.
- 저장소에 정의된 포맷터, 린터, 타입 검사, 테스트를 변경 범위에 맞게 실행한다.
- UI 변경은 390×844 모바일 화면 기준의 레이아웃, 줄바꿈, clipping, overflow를 확인한다.

## Git 규칙

- 커밋과 푸시는 사용자가 명시한 경우에만 수행한다.
- 커밋 전 staged diff와 대상 경로를 확인한다.
- 커밋 메시지는 `<type>: <scope>` 형식을 사용한다.
- 자체 작업 PR은 CI 초록 + 리뷰 완료 상태면 사용자에게 매번 확인받지 않고 merge한다(2026-09-21 사용자 지시). deploy·비밀값 변경·worktree 삭제는 계속 별도 승인 대상이다.
